1. SELECT * FROM users;

2. CREATE DATABASE first_program;

3. \l or \list (you can see how many databases you have through this command)

4. DROP DATABASE first_program;

5. /! cls (Clear terminal)

6. CREATE TABLE users(
    id INT PRIMARY KEY,
    name VARCHAR(50),
    email VARCHAR(50),
    password VARCHAR(50),
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
)

7. ALTER TABLE users ADD COLUMN age INT;

8.ALTER TABLE users DROP COLUMN age;

9. INSERT INTO users (id, name, email, password) VALUES (1, 'John Doe', 'john.doe@example.com', 'password123');

10. UPDATE users SET name = 'Jane Doe' WHERE id = 1;

11. DELETE FROM users WHERE id = 1;

12. SELECT name, email FROM users WHERE id = 1;

13. SELECT COUNT(*) FROM users;

14. SELECT * FROM users WHERE name LIKE 'J%';

15. SELECT * FROM users ORDER BY created_at DESC;

16. SELECT * FROM users LIMIT 10 OFFSET 5;

17. SELECT * FROM users WHERE email IS NOT NULL;

18. SELECT * FROM users WHERE created_at BETWEEN '2023-01-01' AND '2023-12-31';

19. SELECT * FROM users WHERE name IN ('John Doe', 'Jane Doe');

20. SELECT * FROM users WHERE name IS NULL;

21. SELECT * FROM users WHERE name IS NOT NULL;

22. SELECT * FROM users WHERE name = 'John Doe' AND email = 'john.doe@example.com';

23. ALTER DATABASE first_program RENAME TO second_program;

24. ALTER TABLE users RENAME TO customers;

25. ALTER TABLE customers RENAME COLUMN name TO full_name;

26. ALTER TABLE customers RENAME COLUMN email TO email_address;

 
