# Day 01 - PostgreSQL Setup

## Objective

Learn how PostgreSQL works and verify the installation.

## What is PostgreSQL?

PostgreSQL is an open-source relational database management system (RDBMS).

It is used to store, organize, and retrieve data efficiently.

## Verify Installation

Run the following command:

```sql
SELECT version();
```